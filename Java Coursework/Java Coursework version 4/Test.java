import java.io.IOException;

public class Test {
    // Generating Rooms for future use
    public static void main(String[] args) throws IOException {
/*         do {
            
        
        int x = 52, y = 64;
        boolean xLeap=true;
        Boolean TruX = null;
        int x2=x;

        List <Integer> Duration = new ArrayList<>();     
        List <Integer> Duration2 = new ArrayList<>();
        for (int j = 0; x != y; j++) {
                

                Duration.add(x);
                x++;

            }
        
        for (int j = 0; x2 != y; j++) {
                

                Duration2.add(x2);
                x2++;

            }
        System.out.println(Duration.size());
        System.out.println(Duration2.size());
        for (int i = 0; i <= Duration.size()-1; i++) { // if a date matches another date then overlap will be true
            
            for (int j = 0; j <= Duration2.size()-1; j++) {
                System.out.println(Duration.get(i)+"-"+Duration2.get(j));
                if (Objects.equals(Duration.get(i), Duration2.get(j))){
                    TruX = true;
                    break;
                }
            }
        }
        System.out.println(TruX);
    } while (true); */
     int [] num = {1,2,3};
     for (Object ex : num) {
         System.out.println(ex);
     } 
    }
}